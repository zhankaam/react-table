import { styled } from "@stitches/react";

const Container = styled("div", {
    display: 'flex',
    justifyContent: 'space-evenly',
    marginTop: '20px',
    flexWrap: 'wrap',
    gap: '10px'
});

export {Container};
